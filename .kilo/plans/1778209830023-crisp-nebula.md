# Test Suite Improvement Plan

## Goal
Perbaiki test case yang ada dan buat test case baru untuk semua fitur yang belum memiliki test coverage, khususnya untuk sistem slot management dan kalender yang baru diimplementasikan.

---

## Current Test Coverage Analysis

### ✅ Existing Tests (Working)
1. **PreorderTabsTest** (Feature/Livewire) - 10 tests
   - Tab navigation & filtering
   - Order status transitions
   - Cancel & confirm received
   - Helper methods (title, description, qty, total)
   - Payment redirect

2. **CartTest** (Feature/Livewire) - 6 tests
   - Session cart normalization
   - Add/increment items
   - Toggle & total calculation
   - Decrement & remove
   - Checkout redirect

3. **ModelCoreTest** (Unit/Models) - 11 tests
   - Address relationships
   - PreOrder date casting & relationships
   - PreOrderDetail relationships
   - Product image accessor
   - Promo percentage calculation
   - User password hashing

4. **LandingPageControllerTest** (Feature/Controllers) - Basic test

### ❌ Missing Test Coverage

#### Critical Features Without Tests:

1. **TanggalTersedia Model** (NEW - Slot Management)
   - Accessor: `terisi` (counts orders including unpaid)
   - Accessor: `sisa_kuota` (kuota - terisi)
   - Accessor: `status` (Aktif/Penuh/Nonaktif)
   - Scope: `aktif()` (is_aktif = true)
   - Scope: `mendatang()` (tanggal >= today)
   - Relationship: `preOrders()` (hasMany via actual_periode)

2. **CheckoutController** (CRITICAL - Slot Locking)
   - Slot validation with lockForUpdate()
   - Race condition prevention
   - Order creation with unpaid status
   - Slot availability check
   - Transaction rollback on slot full

3. **TanggalController** (NEW - Calendar)
   - `kalender()` method
   - Month navigation
   - Orders grouping by date
   - Slot data integration
   - Statistics calculation

4. **Api\TanggalTersediaController** (NEW - API)
   - `index()` - Get available dates
   - `check()` - Check specific date availability
   - Response format validation

5. **SlotController** (Admin CRUD)
   - `index()` - List slots
   - `store()` - Create slot
   - `update()` - Update slot
   - `destroy()` - Delete slot
   - `toggle()` - Toggle is_aktif

6. **CartController**
   - `storeProduct()` - Add product to cart
   - `storePromo()` - Add promo to cart
   - Session management

7. **MidtransWebhookController** (Payment)
   - Webhook signature validation
   - Order status update
   - Payment confirmation

---

## Implementation Plan

### Phase 1: Fix Existing Tests (if broken)
**Priority: HIGH**

1. Run existing test suite
2. Fix any failing tests
3. Update assertions if business logic changed

**Files to check:**
- `tests/Feature/Livewire/PreorderTabsTest.php`
- `tests/Feature/Livewire/CartTest.php`
- `tests/Unit/Models/ModelCoreTest.php`

### Phase 2: Unit Tests for TanggalTersedia Model
**Priority: CRITICAL**

Create: `tests/Unit/Models/TanggalTersediaTest.php`

**Test Cases:**
1. ✅ `test_terisi_counts_orders_with_all_relevant_statuses()`
   - Create orders with statuses: unpaid, paid, processing, shipping, completed
   - Assert terisi = 5
   - Create order with status 'cancelled'
   - Assert terisi still = 5 (cancelled not counted)

2. ✅ `test_sisa_kuota_calculates_correctly()`
   - Create slot with kuota = 10
   - Create 3 orders
   - Assert sisa_kuota = 7
   - Create 7 more orders
   - Assert sisa_kuota = 0

3. ✅ `test_sisa_kuota_never_goes_negative()`
   - Create slot with kuota = 5
   - Create 10 orders
   - Assert sisa_kuota = 0 (not -5)

4. ✅ `test_status_returns_nonaktif_when_is_aktif_false()`
   - Create slot with is_aktif = false, kuota = 10, no orders
   - Assert status = 'Nonaktif'

5. ✅ `test_status_returns_penuh_when_sisa_kuota_zero()`
   - Create slot with kuota = 5
   - Create 5 orders
   - Assert status = 'Penuh'

6. ✅ `test_status_returns_aktif_when_available()`
   - Create slot with is_aktif = true, kuota = 10
   - Create 3 orders
   - Assert status = 'Aktif'

7. ✅ `test_aktif_scope_filters_active_slots()`
   - Create 3 slots: 2 active, 1 inactive
   - Query with aktif() scope
   - Assert count = 2

8. ✅ `test_mendatang_scope_filters_future_dates()`
   - Create 3 slots: 1 past, 1 today, 1 future
   - Query with mendatang() scope
   - Assert count = 2 (today + future)

9. ✅ `test_pre_orders_relationship_works()`
   - Create slot
   - Create 3 orders with actual_periode = slot.tanggal
   - Assert slot->preOrders()->count() = 3

### Phase 3: Feature Tests for CheckoutController
**Priority: CRITICAL**

Create: `tests/Feature/Controllers/CheckoutControllerTest.php`

**Test Cases:**
1. ✅ `test_checkout_index_requires_authentication()`
   - GET /checkout without auth
   - Assert redirect to login

2. ✅ `test_checkout_index_shows_cart_items()`
   - Add items to session cart
   - GET /checkout
   - Assert items displayed

3. ✅ `test_checkout_store_validates_actual_periode_required()`
   - POST /checkout without actual_periode
   - Assert validation error

4. ✅ `test_checkout_store_rejects_nonexistent_date()`
   - POST /checkout with date not in tanggal_tersedia
   - Assert error message

5. ✅ `test_checkout_store_rejects_inactive_slot()`
   - Create slot with is_aktif = false
   - POST /checkout with that date
   - Assert error message

6. ✅ `test_checkout_store_rejects_full_slot()`
   - Create slot with kuota = 2
   - Create 2 orders for that date
   - POST /checkout with that date
   - Assert error "Slot penuh"

7. ✅ `test_checkout_store_creates_order_with_unpaid_status()`
   - Create slot with available quota
   - POST /checkout
   - Assert order created with status = 'unpaid'

8. ✅ `test_checkout_store_locks_slot_immediately()`
   - Create slot with kuota = 5
   - POST /checkout (creates order)
   - Refresh slot
   - Assert terisi = 1, sisa_kuota = 4

9. ✅ `test_checkout_store_prevents_race_condition()`
   - Create slot with kuota = 1
   - Simulate 2 concurrent requests (use DB transactions)
   - Assert only 1 order created
   - Assert 2nd request gets "Slot penuh" error

10. ✅ `test_checkout_store_creates_order_details()`
    - Add 3 items to cart
    - POST /checkout
    - Assert 3 PreOrderDetail records created

11. ✅ `test_checkout_store_redirects_to_midtrans()`
    - POST /checkout
    - Assert redirect to payment URL

### Phase 4: Feature Tests for TanggalController
**Priority: HIGH**

Create: `tests/Feature/Controllers/TanggalControllerTest.php`

**Test Cases:**
1. ✅ `test_kalender_requires_admin_authentication()`
   - GET /admin/jadwal/kalender without auth
   - Assert redirect to login

2. ✅ `test_kalender_shows_current_month_by_default()`
   - GET /admin/jadwal/kalender
   - Assert bulan = current month
   - Assert tahun = current year

3. ✅ `test_kalender_accepts_month_year_parameters()`
   - GET /admin/jadwal/kalender?bulan=3&tahun=2026
   - Assert bulan = 3, tahun = 2026

4. ✅ `test_kalender_groups_orders_by_date()`
   - Create 3 orders for date A
   - Create 2 orders for date B
   - GET /admin/jadwal/kalender
   - Assert pesananPerTanggal[A] count = 3
   - Assert pesananPerTanggal[B] count = 2

5. ✅ `test_kalender_includes_slot_data()`
   - Create 2 slots for current month
   - GET /admin/jadwal/kalender
   - Assert tanggalTersedia count = 2
   - Assert slot data includes kuota, terisi, sisa, status

6. ✅ `test_kalender_calculates_statistics_correctly()`
   - Create 5 orders
   - Create 3 active slots, 1 full slot
   - GET /admin/jadwal/kalender
   - Assert totalPesanan = 5
   - Assert slotTersedia = 3
   - Assert slotPenuh = 1

7. ✅ `test_kalender_identifies_busiest_date()`
   - Create 1 order for date A
   - Create 5 orders for date B
   - Create 2 orders for date C
   - GET /admin/jadwal/kalender
   - Assert tanggalRamaiFmt = date B formatted

### Phase 5: Feature Tests for API Controller
**Priority: HIGH**

Create: `tests/Feature/Api/TanggalTersediaControllerTest.php`

**Test Cases:**
1. ✅ `test_index_returns_available_dates()`
   - Create 3 active slots with sisa > 0
   - Create 1 inactive slot
   - Create 1 full slot
   - GET /api/tanggal-tersedia
   - Assert response has 3 dates (only active with sisa > 0)

2. ✅ `test_index_response_format_is_correct()`
   - Create 1 slot
   - GET /api/tanggal-tersedia
   - Assert JSON structure matches expected format
   - Assert fields: id, tanggal, tanggal_display, kuota, terisi, sisa, status, is_available, keterangan

3. ✅ `test_index_filters_by_date_range()`
   - Create slots for Jan, Feb, Mar
   - GET /api/tanggal-tersedia?start_date=2026-02-01&end_date=2026-02-28
   - Assert only Feb slots returned

4. ✅ `test_check_returns_availability_for_specific_date()`
   - Create slot for 2026-05-15
   - GET /api/tanggal-tersedia/check?tanggal=2026-05-15
   - Assert available = true

5. ✅ `test_check_returns_false_for_full_slot()`
   - Create slot with kuota = 2
   - Create 2 orders
   - GET /api/tanggal-tersedia/check?tanggal=...
   - Assert available = false

6. ✅ `test_check_validates_tanggal_parameter()`
   - GET /api/tanggal-tersedia/check (no tanggal)
   - Assert 422 validation error

### Phase 6: Feature Tests for SlotController
**Priority: MEDIUM**

Create: `tests/Feature/Controllers/SlotControllerTest.php`

**Test Cases:**
1. ✅ `test_index_lists_all_slots()`
   - Create 5 slots
   - GET /admin/jadwal/slot
   - Assert 5 slots displayed

2. ✅ `test_store_creates_new_slot()`
   - POST /admin/jadwal/slot with valid data
   - Assert slot created in database

3. ✅ `test_store_validates_required_fields()`
   - POST /admin/jadwal/slot with missing fields
   - Assert validation errors

4. ✅ `test_update_modifies_existing_slot()`
   - Create slot
   - PUT /admin/jadwal/slot/{id} with new data
   - Assert slot updated

5. ✅ `test_destroy_deletes_slot()`
   - Create slot
   - DELETE /admin/jadwal/slot/{id}
   - Assert slot deleted

6. ✅ `test_toggle_switches_is_aktif_status()`
   - Create slot with is_aktif = true
   - POST /admin/jadwal/slot/{id}/toggle
   - Assert is_aktif = false

### Phase 7: Feature Tests for CartController
**Priority: MEDIUM**

Create: `tests/Feature/Controllers/CartControllerTest.php`

**Test Cases:**
1. ✅ `test_store_product_adds_to_session_cart()`
   - POST /cart/{product}
   - Assert session has cart item

2. ✅ `test_store_promo_adds_to_session_cart()`
   - POST /cart/promo/{promo}
   - Assert session has promo item

3. ✅ `test_store_validates_quantity()`
   - POST /cart/{product} with qty = 0
   - Assert validation error

4. ✅ `test_store_increments_existing_item()`
   - Add product to cart
   - POST /cart/{product} again
   - Assert qty = 2

### Phase 8: Integration Tests for Slot Locking
**Priority: CRITICAL**

Create: `tests/Feature/Integration/SlotLockingTest.php`

**Test Cases:**
1. ✅ `test_concurrent_checkouts_respect_slot_limit()`
   - Create slot with kuota = 3
   - Simulate 5 concurrent checkout requests
   - Assert only 3 orders created
   - Assert 2 requests rejected with "Slot penuh"

2. ✅ `test_unpaid_orders_lock_slots()`
   - Create slot with kuota = 5
   - Create 2 unpaid orders
   - Assert terisi = 2, sisa = 3

3. ✅ `test_cancelled_orders_do_not_count_toward_terisi()`
   - Create slot with kuota = 5
   - Create 2 orders, cancel 1
   - Assert terisi = 1 (only non-cancelled)

4. ✅ `test_slot_status_updates_dynamically()`
   - Create slot with kuota = 2
   - Assert status = 'Aktif'
   - Create 2 orders
   - Refresh slot
   - Assert status = 'Penuh'

---

## Test Structure & Best Practices

### Directory Structure
```
tests/
├── Feature/
│   ├── Controllers/
│   │   ├── CheckoutControllerTest.php (NEW)
│   │   ├── TanggalControllerTest.php (NEW)
│   │   ├── SlotControllerTest.php (NEW)
│   │   ├── CartControllerTest.php (NEW)
│   │   └── LandingPageControllerTest.php (existing)
│   ├── Api/
│   │   └── TanggalTersediaControllerTest.php (NEW)
│   ├── Integration/
│   │   └── SlotLockingTest.php (NEW)
│   └── Livewire/
│       ├── PreorderTabsTest.php (existing)
│       └── CartTest.php (existing)
├── Unit/
│   └── Models/
│       ├── TanggalTersediaTest.php (NEW)
│       └── ModelCoreTest.php (existing)
└── TestCase.php
```

### Helper Methods to Add to TestCase.php

```php
// Create authenticated user
protected function createAuthenticatedUser(array $attributes = []): User
{
    $user = User::factory()->create($attributes);
    $this->actingAs($user);
    return $user;
}

// Create admin user
protected function createAdmin(): User
{
    return $this->createAuthenticatedUser(['role' => 'admin']);
}

// Create slot with orders
protected function createSlotWithOrders(int $kuota, int $orderCount, array $slotAttributes = []): TanggalTersedia
{
    $slot = TanggalTersedia::factory()->create(array_merge([
        'kuota' => $kuota,
    ], $slotAttributes));
    
    PreOrder::factory()->count($orderCount)->create([
        'actual_periode' => $slot->tanggal,
        'status' => 'unpaid',
    ]);
    
    return $slot->fresh();
}

// Add items to session cart
protected function addToCart(User $user, array $items): void
{
    session()->put('cart_user_' . $user->id, $items);
}
```

### Factories to Create/Update

1. **TanggalTersediaFactory** (NEW)
```php
public function definition(): array
{
    return [
        'tanggal' => $this->faker->dateTimeBetween('now', '+2 months'),
        'kuota' => $this->faker->numberBetween(5, 20),
        'keterangan' => $this->faker->optional()->sentence(),
        'is_aktif' => true,
    ];
}

public function inactive(): static
{
    return $this->state(fn (array $attributes) => [
        'is_aktif' => false,
    ]);
}

public function full(): static
{
    return $this->afterCreating(function (TanggalTersedia $slot) {
        PreOrder::factory()->count($slot->kuota)->create([
            'actual_periode' => $slot->tanggal,
            'status' => 'unpaid',
        ]);
    });
}
```

### Assertions to Use

- `assertDatabaseHas()` / `assertDatabaseMissing()`
- `assertDatabaseCount()`
- `assertRedirect()`
- `assertStatus()`
- `assertJson()` / `assertJsonStructure()`
- `assertSessionHas()` / `assertSessionMissing()`
- Custom: `assertSlotLocked()`, `assertSlotAvailable()`

---

## Execution Order

1. **Phase 1** - Fix existing tests (if needed)
2. **Phase 2** - TanggalTersedia model tests (foundation)
3. **Phase 3** - CheckoutController tests (critical path)
4. **Phase 8** - Integration tests for slot locking (verify critical feature)
5. **Phase 4** - TanggalController tests (calendar)
6. **Phase 5** - API tests (frontend integration)
7. **Phase 6** - SlotController tests (admin CRUD)
8. **Phase 7** - CartController tests (supporting feature)

---

## Success Criteria

✅ All existing tests pass
✅ New tests cover all critical slot management features
✅ Test coverage for TanggalTersedia model: 100%
✅ Test coverage for CheckoutController slot logic: 100%
✅ Integration tests verify race condition prevention
✅ All tests use factories (no manual model creation)
✅ Tests are isolated (use RefreshDatabase)
✅ Tests run fast (< 30 seconds total)

---

## Questions for User

1. **Test Database**: Apakah konfigurasi SQLite in-memory sudah cukup atau perlu MySQL test database?

2. **Mocking Midtrans**: Untuk test CheckoutController, apakah perlu mock Midtrans API atau cukup test sampai order creation?

3. **Concurrent Test**: Untuk test race condition, apakah perlu test dengan actual concurrent requests atau cukup simulate dengan DB transactions?

4. **Coverage Target**: Apakah ada target minimum code coverage (misal 80%)?

5. **CI/CD**: Apakah tests akan dijalankan di CI/CD pipeline? Jika ya, perlu setup GitHub Actions?

---

## Estimated Effort

- Phase 1: 30 minutes
- Phase 2: 2 hours (9 tests)
- Phase 3: 3 hours (11 tests)
- Phase 4: 2 hours (7 tests)
- Phase 5: 1.5 hours (6 tests)
- Phase 6: 1.5 hours (6 tests)
- Phase 7: 1 hour (4 tests)
- Phase 8: 2 hours (4 tests)

**Total: ~13.5 hours**

---

## Notes

- Semua test menggunakan `RefreshDatabase` trait untuk isolation
- Factory pattern untuk semua model creation
- Descriptive test names yang explain what is being tested
- Arrange-Act-Assert pattern untuk clarity
- Test edge cases (boundary conditions, error states)
- Test happy path dan sad path
